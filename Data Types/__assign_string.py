import numpy as np

#Create Array of strings assigned to the
arr = np.array([0,1,2,3,4,5], dtype='S')

#Now try print the Array and see
print(arr)
#output => [b'0' b'1' b'2' b'3' b'4' b'5']

#print data type of the arres
print(arr.dtype)
#output => |S1